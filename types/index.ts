export type Pokemon = {
	name: string;
	image: string;
	index: number;
};
export type PokemonUrl = {
	name: string;
};
export type LayoutType = {
	title: string;
	keywords?: string;
	description?: string;
	children: any;
};
