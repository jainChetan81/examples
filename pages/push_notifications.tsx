import { Layout } from "../components";

const Push_notifications = () => {

	return (
		<Layout title="Push Notifications">
			<section className="notifications" style={{ display: "grid", height: "100vh", placeContent: "center" }}>
				
			</section>
		</Layout>
	);
};

export default Push_notifications;
